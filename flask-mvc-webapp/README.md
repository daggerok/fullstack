# flask-mvc-webapp

```bash
mkdir flask-mvc-webapp
cd flask-mvc-webapp
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt -r requirements-dev.txt
pytest tests.py
python3 app.py
curl 0:5000/
```
